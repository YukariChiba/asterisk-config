[ext-peer]

exten => sms,5,NoOp()

exten => _+04240803.,5,GotoIf($["${CHANNEL(channeltype)}" = "Message"]?skipanswer)
 same =>            n,Answer()
 same =>            n(skipanswer),GotoIf($["${EXTEN:9}" : ".*[^0-9]+.*"]?ext-invalid,invalidinput,5:)
 same =>            n,Goto(ext-local-0803,${EXTEN:9},5)

exten => _+42400803.,5,GotoIf($["${CHANNEL(channeltype)}" = "Message"]?skipanswer)
 same =>            n,Answer()
 same =>            n(skipanswer),GotoIf($["${EXTEN:9}" : ".*[^0-9]+.*"]?ext-invalid,invalidinput,5:)
 same =>            n,Goto(ext-local-0803,${EXTEN:9},5)

exten => _+04241331.,5,GotoIf($["${CHANNEL(channeltype)}" = "Message"]?skipanswer)
 same =>            n,Answer()
 same =>            n(skipanswer),GotoIf($["${EXTEN:9}" : ".*[^0-9]+.*"]?ext-invalid,invalidinput,5:)
 same =>            n,Goto(ext-local-1331,${EXTEN:9},5)

exten => _+42401331.,5,GotoIf($["${CHANNEL(channeltype)}" = "Message"]?skipanswer)
 same =>            n,Answer()
 same =>            n(skipanswer),GotoIf($["${EXTEN:9}" : ".*[^0-9]+.*"]?ext-invalid,invalidinput,5:)
 same =>            n,Goto(ext-local-1331,${EXTEN:9},5)

exten => _+04243618.,5,NoOp()
 same =>            n,GotoIf($["${CHANNEL(channeltype)}" = "Message"]?sms)
 same =>            n,Dial(PJSIP/${EXTEN}@peer-3618-jerry)
 same =>            n,Goto(ext-invalid,${DIALSTATUS},5)
 same =>            n(sms),GoSub(sub-sms-send,s,1(pjsip:peer-3618-jerry/sip:${EXTEN}@peer-3618-jerry))

exten => _+42403618.,5,NoOp()
 same =>            n,GotoIf($["${CHANNEL(channeltype)}" = "Message"]?sms)
 same =>            n,Dial(PJSIP/${EXTEN}@peer-3618-jerry)
 same =>            n,Goto(ext-invalid,${DIALSTATUS},5)
 same =>            n(sms),GoSub(sub-sms-send,s,1(pjsip:peer-3618-jerry/sip:${EXTEN}@peer-3618-jerry))

exten => _+04242547.,5,NoOp()
 same =>            n,GotoIf($["${CHANNEL(channeltype)}" = "Message"]?sms)
 same =>            n,Dial(PJSIP/${EXTEN}@peer-2547-lantian)
 same =>            n,Goto(ext-invalid,${DIALSTATUS},5)
 same =>            n(sms),GoSub(sub-sms-send,s,1(pjsip:peer-2547-lantian/sip:${EXTEN}@peer-2547-lantian))

exten => _+42402547.,5,NoOp()
 same =>            n,GotoIf($["${CHANNEL(channeltype)}" = "Message"]?sms)
 same =>            n,Dial(PJSIP/${EXTEN}@peer-2547-lantian)
 same =>            n,Goto(ext-invalid,${DIALSTATUS},5)
 same =>            n(sms),GoSub(sub-sms-send,s,1(pjsip:peer-2547-lantian/sip:${EXTEN}@peer-2547-lantian))

exten => _+04243322.,5,NoOp()
 same =>            n,GotoIf($["${CHANNEL(channeltype)}" = "Message"]?sms)
 same =>            n,Dial(PJSIP/${EXTEN}@peer-3322-vconet)
 same =>            n,Goto(ext-invalid,${DIALSTATUS},5)
 same =>            n(sms),GoSub(sub-sms-send,s,1(pjsip:peer-3322-vconet/sip:${EXTEN}@peer-3322-vconet))

exten => _+42403322.,5,NoOp()
 same =>            n,GotoIf($["${CHANNEL(channeltype)}" = "Message"]?sms)
 same =>            n,Dial(PJSIP/${EXTEN}@peer-3322-vconet)
 same =>            n,Goto(ext-invalid,${DIALSTATUS},5)
 same =>            n(sms),GoSub(sub-sms-send,s,1(pjsip:peer-3322-vconet/sip:${EXTEN}@peer-3322-vconet))

exten => _+X.,5,NoOp()
 same =>            n,Goto(ext-enum,${EXTEN},5)

; Catch Invalid
exten => _X!,5,Goto(ext-invalid,invalidinput,5)
exten => i,5,Goto(ext-invalid,invalidinput,5)
