[global]
type=global
use_callerid_contact=yes
endpoint_identifier_order=auth_username,username,ip

[transport4]
type=transport
protocol=udp
bind=0.0.0.0:5060

[transport6]
type=transport
protocol=udp
bind=[::]:5060

[transport-wss]
type=transport
protocol=wss
bind=[::]

#include pjsip/template/*.conf
#include pjsip/auth/*.conf

#include pjsip/peers/*.conf
#include pjsip/phones/*.conf
#include pjsip/modems/*.conf
#include pjsip/pstn/*.conf
#include pjsip/webrtc/*.conf

#include pjsip/enum/*.conf
